# 更新日志

本项目的所有重大变更与版本发布均记录于此文件中。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)，并遵循 [语义化版本（Semantic Versioning）](https://semver.org/lang/zh-CN/)。

---

## [2.0.1] - 2026-08-26

### 新增功能
* **引擎二进制快照（Engine Snapshot）**：新增引擎二进制快照持久化机制（`engine_snapshot.cpp`），支持词典与拼音引擎结构的秒级预热与只读映射恢复，大幅降低多宿主进程冷启动耗时。
* **直接文本上屏请求（DirectTextCommitRequest）**：新增直接文本上屏请求通信通道（`direct_text_commit_request.cpp` 与 `DirectTextCommitRequestStore.cs`），支持剪贴板历史与快速面板直接将文本安全上屏至当前活跃的 TSF 编辑会话。
* **AppContainer 沙箱安全访问控制**：增强私有 DACL 与沙箱权限适配（`private_acl.cpp` 与 `AppContainerAccess.cs`），保证 Windows UWP / 沙箱进程在严格隔离环境下安全读取系统词库与共享模型。
* **剪贴板富文本与图像直通**：重构 `ClipboardImageService.cs` 与 `ClipboardPasteService.cs`，完善 `v` 模式下图片及多格式剪贴板内容的快速直贴与焦点自适应恢复。
* **白霜词库全量集成**：全量导入 Rime Frost 白霜拼音词库，包含约 67.7 万多字词条与 8,247 条单字拼音读音。
* **分级回退语言模型**：支持多级语言模型加载机制（完整墨奇模型 → `system_ngram.bin` → 字符先验），在未安装大模型时自动平滑回退，保障基础词间搭配排序。
* **单字全量召回与先验排序**：引入 `system_lexeme_prior.bin`，优化单字与双字的无上下文常用度排序，大幅提升生僻字与常用字的命中率。
* **通用纠错增强**：增强全拼常见击键错误（如相邻键误触、声韵母混淆）的模糊容错匹配。
* **自包含安装包**：提供基于 NSIS 的单文件安装程序，内置运行时依赖检查与静默卸载支持。
* **英文词库与混拼**：集成 `rime-easy-en` 与 ECDICT 派生英文词库，优化中英混拼排序与长词联想。

### 修复与优化
* **候选窗闪烁修复**：重构候选框刷新与渲染管线，消除高打字速度下的逐键渲染闪跳。
* **光标跟随与多屏适配**：修复部分复杂宿主应用中候选框位置跟随偏移或跨屏 DPI 缩放时的显示异常。
* **SSF 皮肤兼容性**：修复皮肤挂件动画错位与 DirectWrite 文本绘制边缘抗锯齿效果。
* **全拼/双拼切换**：修复使用快捷键切换全拼与双拼时的状态即时同步问题。
* **安装后语言列表同步**：Setup 注册 TSF 组件后同步写入当前用户的简体中文语言列表，
  卸载时移除对应条目；首次安装失败会回滚本次新增的语言列表状态。
* **便携版错误处理**：Portable 安装脚本在用户语言配置或快捷方式创建失败时不再误报成功，
  并回滚已经完成的 COM/TSF 注册。
* **发行完整性校验**：Setup 与 Portable 共用同一套版本、清单、SHA-256、法律声明和禁入
  文件验证，GitHub Release 同时提供两个发行包及对应校验文件。

---

## [2.0.0] - 2026-08-10

### 新增功能
* **现代 WPF 设置中心**：推出全新的自包含 WPF 设置程序，支持常规选项、双拼方案选择、自定义短语管理、词库导入与隐私策略配置。
* **词库版本化管理**：引入词库 Manifest 校验与多版本并行共存机制，支持平滑升级与版本回退。
* **自定义短语系统**：支持用户自定义短语与快捷输入映射（`custom_phrases.txt`）。
* **字数统计模块**：本地轻量化字数与击键统计，仅记录按键计数与日期，严格保护用户隐私，绝不记录任何输入正文。

### 架构改进
* **TSF / COM 接口解耦**：将 Windows 文本服务框架（TSF）生命周期管理与拼音引擎打分核心彻底分离。
* **高性能双数组检索**：基于 Darts-clone 双数组 Trie 树实现词典索引，实现微秒级前缀匹配。

---

## [0.4.4] - 2026-07-20

### 新增功能
* **初始 MVP 原型发布**：实现 Windows TSF 基础服务注册与文字上屏功能。
* **Win32 无焦点候选窗**：实现基础横排/竖排候选词窗口绘制。
* **基础词库检索**：支持基础拼音切分、全拼匹配与首选词上屏。
* **基础辅助脚本**：提供环境检查（`check_env.ps1`）、构建脚本（`build.ps1`）与安装注册脚本。
