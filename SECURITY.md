# 安全政策

我们高度重视财神输入法（Caishen IME）的安全性与用户隐私保护。作为系统底层的输入法服务，我们承诺提供纯净、透明、安全的本地输入体验。

---

## 一、 支持的版本

以下版本目前正在接受安全更新支持：

| 版本 | 是否支持安全更新 | 说明 |
| :--- | :--- | :--- |
| `2.0.x` | :white_check_mark: 支持 | 当前稳定维护版本（主分支） |
| `< 2.0.0` | :x: 不再支持 | 早期原型版本，建议升级至最新版本 |

---

## 二、 核心安全与隐私原则

1. **纯本地离线运行**：输入法所有拼音查询、词库检索、语言模型计算及字数统计均在本地完成，无任何网络请求模块或后门，绝不上传用户的任何击键或输入文本。
2. **严格的访问控制（ACL）**：输入法在多用户/提权（UAC）或 Windows AppContainer 沙箱环境中运行时，遵循严格的只读词库共享与最小权限隔离原则。
3. **内容日志默认关闭**：仅在开发者模式或显式调试时在本地输出诊断信息，发布版本默认禁用所有输入正文捕获。

---

## 三、 如何报告安全漏洞

如果你在本项目中发现了任何潜在的安全漏洞（如权限提升漏洞、DLL 劫持风险、缓冲区溢出或敏感数据泄露风险等），**请不要直接在公开的 Issue 列表中讨论或披露**。

请按照以下流程私下联系我们：

1. **报告渠道**：
   - 方式一：通过 GitHub 仓库的 **Security Advisories**（安全通告）功能创建私密漏洞报告（点击仓库顶部的 `Security` → `Report a vulnerability`）。
   - 方式二：发送邮件至项目维护者邮箱（请在邮件标题注明 `【安全漏洞报告】财神输入法 - <漏洞简述>`）。
2. **报告内容建议包含**：
   - 漏洞类型与受影响的组件/文件（如 `ShuruIme.dll`、安装程序或设置中心）
   - 漏洞危害评估与触发条件
   - 详细的复现步骤或概念验证（PoC）
   - 可能的修复建议（若有）

---

## 四、 漏洞处置流程与时效承诺

* **确认与响应**：我们将在收到漏洞报告后的 **48 小时内** 进行初步确认并回复。
* **评估与修复**：确认漏洞存在后，我们将优先排期修复，并在私有分支完成验证与回归测试。
* **发布与致谢**：修复完成后，我们将发布包含安全修复的补丁版本，并在 GitHub Release 和更新日志中对漏洞发现者进行公开鸣谢（可按要求保持匿名）。
